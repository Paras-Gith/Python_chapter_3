name = "paras"
print(len(name)) # len is used for measuring length of a string

print(name.endswith("as")) # .endwith is used for checking the last character of a sting
print(name.startswith("as")) # .startwith is used for checking the first character of a
