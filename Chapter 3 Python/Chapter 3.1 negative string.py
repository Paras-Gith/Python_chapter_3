name = "paras"

print(name[-4:-1]) # now the sting is asking the user which character is -4 and -1
#to check the character for above characters we simply change it to positive numbers
print(name[1:4]) #also swap the place between them
